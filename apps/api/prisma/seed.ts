// Seed de conveniência para desenvolvimento local. Não é o mecanismo de
// categorias padrão por usuário (isso acontece via UsersService.syncFromClaims
// no primeiro login real, ver src/users) — este script fica vazio até
// existirem fixtures de dev que valha a pena versionar aqui.
async function main() {}

main();
