VITE_SUPABASE_URL=            # mesmo valor de SUPABASE_URL em apps/api/.env
VITE_SUPABASE_ANON_KEY=       # painel Supabase → Project Settings → API → "anon public" (nunca a service_role key)
VITE_API_BASE_URL=http://localhost:3000
